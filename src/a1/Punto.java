package a1;

/*
 * Crea un programa con una clase llamada Punto que representará
 * un punto de dos dimensionesen un plano. Solo contendrá dos atributos
 * enteros llamadas x e y (coordenadas).En el main de la clase principal
 * instancia 3 objetos Punto con las coordenadas (5,0), (10,10) y (-3,7).
 * Muestra por pantalla sus coordenadas (utiliza un println para cada punto).
 * Modifica todas lascoordenadas (prueba distintos operadores
 * como =   +   -   +=   *=...) y vuelve a imprimirlas porpantalla.
 */

public class Punto {
	
	int x, y;
}
